/** Fallback when CMS `signupWelcome.message` is empty. */
export const DEFAULT_SIGNUP_WELCOME_MESSAGE = [
  "Thank you for trusting us with something this important.",
  "We'll do our best to make the journey clearer from here.",
].join("\n");

export const DEFAULT_SIGNUP_WELCOME_DURATION_SECONDS = 5;
